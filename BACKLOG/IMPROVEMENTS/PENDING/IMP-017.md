# IMP-017: Tests unitaires pour LocalBusinessCitySchema JSON-LD

**Type:** Improvement
**Statut:** A faire
**Priorite:** Basse
**Complexite:** XS
**Tags:** tests, seo, schema
**Depends on:** none
**Blocked by:** —
**Date creation:** 2026-03-28

---

## Description

Le composant `LocalBusinessCitySchema` n'a aucun test. Un unit test devrait verifier que le JSON-LD genere utilise toujours `SITE.address.city` pour `addressLocality` (et non le `city` prop), et que `areaServed.name` utilise bien le `city` prop.

Signale par le worker BUG-005 (boy scout).

## Fichiers concernes

- `src/components/seo/__tests__/LocalBusinessCitySchema.test.tsx` — a creer
- `src/components/seo/LocalBusinessCitySchema.tsx` — composant a tester

## Criteres d'acceptation

- [ ] Test verifie que `addressLocality` est toujours "Les Ponts-de-Ce" quelle que soit la valeur de `city`
- [ ] Test verifie que `areaServed.name` correspond au `city` prop
- [ ] Test verifie que le JSON-LD est valide (parseable)
- [ ] `pnpm test` passe

## Tests de validation

- [ ] `pnpm test`
